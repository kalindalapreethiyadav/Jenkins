pipeline {
//agent { label 'msr' }
  agent any

    stages {
        stage('Hello 1') {
            steps {
                echo 'Hello World'
                sh "curl ifconfig.co"
            }
        }
        stage('Hello 2') {
            steps {
                echo 'Hello World'
                sh "curl ifconfig.co"
            }
        }
        stage('Hello 3') {
            steps {
                echo 'Hello World'
                sh "curl ifconfig.co"
            }
        }
        stage('Hello 4') {
            steps {
                echo 'Hello World'
                sh "curl ifconfig.co"
            }
        }
    }
}
